type teacher {
    id: ID!
    name: String!
    bio: BIO!
    avatar_url: String!
}

type Query {
    teachers: [teacher!]!
}

type Mutation {
    createTeacher(name: String!, bio: String!, avatar_url: String!): teacher!
}